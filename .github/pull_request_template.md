## What this changes

<!-- One or two sentences. If it fixes an issue, link it: Fixes #123 -->

## How it was verified

<!-- Which tests cover it, or what you ran. `pnpm test` output is enough for most changes. -->

- [ ] `pnpm -r build` passes
- [ ] `pnpm -r typecheck` passes
- [ ] `pnpm test` passes

## Contributor License Agreement

- [ ] I have read and agree to the [CLA, v1.0](https://github.com/Karaca7/gnl-framework/blob/main/CLA.md).

<!--
Ticking the box above is how the CLA is accepted — it grants the Maintainer the rights described in
CLA.md, including the right to relicense the contribution. It is a one-time step per GitHub account;
later PRs from the same account are already covered. A PR cannot be merged with the box unticked.

The link is absolute on purpose: a relative path like ../CLA.md resolves against the pull request
URL, not the file tree, and lands on a 404.

If you tick the box and the CLA check does not re-run, edit this description through the "..." menu
and save it — toggling a rendered checkbox does not reliably fire the `edited` event the check
listens for.
-->
