node_modules
dist

# Ordinary hygiene: src/auth.ts and the model setup read provider keys and tokens from
# process.env, and a .env is the usual way to supply them. The scaffold does not create one.
.env
.env.*
!.env.example
*.db
*.db-*
.DS_Store
