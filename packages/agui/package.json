{
  "name": "@gnl/agui",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "description": "AG-UI (CopilotKit) protocol adapter: converts @gnl/server SSE contract to AG-UI event sequences + Hono endpoint factory.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@gnl/server": "workspace:^",
    "hono": "^4.6.0"
  },
  "peerDependencies": {
    "@gnl/durable": "workspace:^"
  },
  "devDependencies": {
    "@gnl/durable": "workspace:*",
    "ai": "^5.0.0",
    "zod": "^3.25.0"
  }
}
